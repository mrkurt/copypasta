<%= raw edit_author %> just submitted a correction for one of your pages:
<%= @edit.url %>

Proposed changes:
<%= raw @edit.proposed %>

Manage edits for this page:
<%= raw new_session_url(:key => @editor.create_token.key, :url => @edit.url) %>
