<%= raw ((@edit.user_name.blank? && '(Anonymous)') || @edit.user_name) %> just submitted a correction for one of your pages:
<%= raw @edit.url %>

Original:
<%= raw @edit.original %>

Proposed changes:
<%= raw @edit.proposed %>

Manage edits for this page:
<%= raw new_session_url(:key => @editor.create_token.key, :url => @edit.url + '#copypasta-auto') %>
